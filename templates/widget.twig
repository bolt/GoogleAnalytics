<div class="panel panel-default panel-news">
    <div class="panel-heading">
        <i class="fa fa-fw fa-area-chart"></i>
        {{__("Visitors/Pageviews in the last 7 days.")}}
    </div>

    <div class="panel-body">
        <div class="lastweek">
            <div id="chart-0-container" class="chart-widget"></div>
            <div class="Chartjs-legend" id="legend-0-container"></div>
            <p><span id="active-users-container"></span></p>
        </div>
        {% if statisticspage %}
            <a class="btn btn-secondary" href="{{ path('displayStatistics') }}">{{__("More statistics")}}</a>
        {% endif %}
    </div>
</div>
<link rel="stylesheet" type="text/css" href="{{ asset(webpath ~ '/styles.css', 'extensions') }}" />
<script>
    var GAtranslations = {
        visitorsrightnow: "{{__("Active visitors right now")}}",
        pageviewslastweek: "{{ __("Pageviews last week") }}",
        pageviewsthisweek: "{{ __("Pageviews this week") }}",
        lastweek: "{{ __("Visits last week") }}",
        thisweek: "{{ __("Visits this week") }}"
    };
    var locale = "{{locale}}";
    var data = {ids: "ga:{{profile}}"};
    var access_token = {{token|raw}};

    (function(w,d,s,g,js,fjs){
        g=w.gapi||(w.gapi={});g.analytics={q:[],ready:function(cb){this.q.push(cb)}};
        js=d.createElement(s);fjs=d.getElementsByTagName(s)[0];
        js.src='https://apis.google.com/js/platform.js';
        fjs.parentNode.insertBefore(js,fjs);js.onload=function(){g.load('analytics')};
    }(window,document,'script'));
</script>
<script src="{{ asset(webpath ~ '/es6-promise.min.js', 'extensions') }}" type="text/javascript"></script>
<script src="{{ asset(webpath ~ '/active-users.js', 'extensions') }}" type="text/javascript"></script>
<script src="{{ asset(webpath ~ '/date-range-selector.js', 'extensions') }}" type="text/javascript"></script>
<script src="{{ asset(webpath ~ '/Chart.min.js', 'extensions') }}" type="text/javascript"></script>
<script src="{{ asset(webpath ~ '/googleanalytics.js', 'extensions') }}" type="text/javascript"></script>
